## Frontend (Vite) environment variables
## Copy to ".env" for local dev, and set in Vercel for production.

# Public backend base URL (NO /api at end, وبدون / في النهاية).
# Local dev can omit this to use Vite proxy "/api" -> http://localhost:5000
VITE_API_URL=http://localhost:5000

